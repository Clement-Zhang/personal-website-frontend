import Default from "../templates/default";

const Nothing = () => {
    return (
        <div>
            <Default />
            <p>
                Nothing here yet.
            </p>
        </div>
    );
}

export default Nothing;